# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D033 | M005 | arch | Transition visuals owned by prefab | Self-contained prefab with Canvas + CanvasGroup + UnityTransitionPlayer MonoBehaviour | User wants future transitions to use images/animations/shaders — prefab must own all visual details. Swapping the prefab changes the look without code changes. | No — foundational for extensible transitions | agent |
| D034 | M005 | library | LitMotion for transition tweening | LitMotion `BindToAlpha`/`ToUniTask()` replaces manual while-loop alpha interpolation | LitMotion already installed as project tweening library; provides correct easing, cancellation, and CanvasGroup binding out of the box | No — LitMotion is the project's tweening standard | agent |
| D035 | M006 | data | ScriptableObjects for meta world data | WorldData → EnvironmentData → RestorableObjectData as ScriptableObjects in Assets/Data/ | Static game data that designers edit — SO is the Unity-native pattern; flat structure with blocked-by lists per user request, not a tree | Yes — could migrate to JSON/Addressables if data volume requires it | agent |
| D036 | M006 | arch | Meta persistence via interface-backed PlayerPrefs | IMetaSaveService interface with PlayerPrefsMetaSaveService implementation using JSON serialization | User wants persistence now but implementation swappable later (file, cloud); PlayerPrefs is simplest for dev; JSON handles structured per-object progress | Yes — swap to file/cloud when needed | agent |
| D037 | M006 | arch | Main screen IS the meta world | No separate MetaWorld screen — MainMenu scene shows environment, objects, balance, play button, settings | User explicitly said "the meta world will be in the main screen"; reduces screen count, puts spending right next to playing | No — core UX decision | agent |
| D038 | M006 | pattern | Tap-to-spend without confirmation | Tapping an unblocked object spends one golden piece for one restoration step — no confirmation popup | User said "no confirmation on tap to spend" — UX will change later, keep it simple now | Yes — add confirmation or animation later | agent |
| D039 | M006 | scope | Stub gameplay: counter + correct/incorrect buttons only | Core gameplay is level ID, piece counter, place-correct/place-incorrect, hearts — no board, tray, or neighbor logic | User explicitly said core gameplay comes later; this milestone proves the flow skeleton | Yes — replaced by real gameplay in a future milestone | agent |
| D040 | M006 | scope | Ad/IAP as tappable UI stubs, not just interfaces | RewardedAd and IAPPurchase get full popup UIs with text stubs, not just C# interfaces | User explicitly said "UI stubs, not only interface stubs" | Yes — replaced by real SDK integration later | agent |
| D041 | M007 | arch | View resolution via IViewResolver in Core | Generic `IViewResolver` with `Get<T>() where T : class` — separate from IPopupContainer | User wants generic resolution on a separate interface. Keeps Core game-agnostic. Container implements both IPopupContainer and IViewResolver. | No | agent |
| D042 | M007 | arch | Popup container renamed to reflect expanded role | UnityPopupContainer renamed (e.g. ViewContainer) to signal it does more than show/hide | User explicitly chose rename over keeping old name | No | agent |
| D043 | M007 | arch | Scene root convention for controller resolution | After additive scene load, find scene controllers by querying loaded scene's root GameObjects — not FindFirstObjectByType | User chose scene root convention over self-registration pattern | No | agent |
| D044 | M007 | arch | GameBootstrapper uses SerializeField refs for boot infrastructure | UnityInputBlocker, UnityTransitionPlayer, container wired via [SerializeField] on GameBootstrapper | User chose explicit refs over scene scanning. All components are in Boot scene — straightforward. | No | agent |
| D045 | M007 | scope | Popup views only — screen views stay in their scenes | Only popup views move to prefab-based container. Screen views (MainMenu, Settings, InGame) remain in their scenes with SerializeField refs. | User explicitly chose popups only for now — screens are a separate concern. | Yes — extend to screens in a future milestone if needed | agent |
| D046 | M007 | arch | Popup container holds inactive children (not instantiation) | Container holds popup prefab instances as inactive children, activated via SetActive — not instantiated on demand | User mentioned instantiation as a future possibility. Inactive children is the stepping stone. Supersedes D017 (pre-instantiated show/hide) only in organization, not mechanism. | Yes — switch to instantiation when popup count grows (R078) | agent |
| D047 | M007/S02 | arch | IViewResolver parameter on scene controller Initialize() is optional (= null default) | Added as optional last parameter `IViewResolver viewResolver = null` on both InGameSceneController.Initialize() and MainMenuSceneController.Initialize() | Preserves backward-compat for all existing call sites; makes null-passing safe in tests where SetViewsForTesting() overrides take precedence; avoids CS7036 errors without requiring every call site updated simultaneously | Yes — make required once S03 ensures all call sites pass a resolver | agent |
| D048 | M007/S02 | pattern | View getter resolution order in scene controllers | Resolution order: override field (SetViewsForTesting test seam) → SerializeField ref (if present) → _viewResolver?.Get<T>() → Debug.LogError + return null | Test seam must take precedence over production resolver to keep tests isolated. SerializeField ref checked before resolver for legacy scene-placed support. Resolver is the general fallback. LogError preserves failure visibility when all sources return null. | No — established resolution order for all scene controller view getters | agent |
| D049 | M008 | arch | Animation layer: PopupViewBase as abstract MonoBehaviour with default LitMotion impls | Abstract base class PopupViewBase : MonoBehaviour with virtual AnimateInAsync/AnimateOutAsync. Defaults: bounce-up in (Y offset + OutBounce), scale+fade out (scale 0.85 + alpha 0, InBack). All popup views inherit it. | User wants overrideable defaults without forcing every view to implement from scratch. Abstract class fits Unity MonoBehaviour constraint; interface alone can't carry default impls in C# at this Unity version. | No | agent |
| D050 | M008 | arch | Blocker fade: single layer repositions, input unblocks at fade-out start | Single UnityInputBlocker instance fades in/out. IInputBlocker gains FadeInAsync/FadeOutAsync. PopupManager calls Block()+FadeInAsync concurrent with ShowPopupAsync, then calls Unblock()+starts FadeOutAsync before awaiting HidePopupAsync. | User confirmed single repositioning layer. Input timing split is a product requirement (R080) — unblock must happen before animation ends. | No | agent |
| D051 | M008 | library | TMP for all new prefab UI components | TextMeshPro for all prefab text and button labels. Existing non-prefab uGUI Text stays until SceneSetup rewrite replaces it. | User explicitly chose TMP. Better typography, standard for mobile. Requires adding TMP to Game and Editor asmdefs. | No | agent |
| D052 | M009 | arch | In-scene screen manager: SetActive panel swap | `InSceneScreenManager<TScreenId>` uses `SetActive(true/false)` on pre-existing panel GameObjects — no scene loading | User explicitly chose instant swap; no animation this milestone; mirrors ScreenManager<T> generic pattern but avoids scene loading overhead | Yes — add animation in a future milestone | agent |
| D053 | M009 | arch | Popup sort order scheme for stacking | Each popup gets its own Canvas with OverrideSorting. Sort order = 50 + (stackDepth * 100). Blocker at 100. Bottom popup at 50 (below blocker), top popup at 150+ (above blocker). | User requirement: bottom popup must be visually dimmed by overlay when a second popup stacks on top. Per-popup canvas is the only clean Unity mechanism. | No | agent |
| D054 | M009 | arch | PopupManager _isOperating guard: keep as-is | The guard blocks *concurrent* re-entrant calls only. Sequential stacking (show B after A's WaitForChoice returns) does not hit the guard. No change needed. | Removing the guard entirely would allow unintended parallel operations. Sequential stacking works naturally with the existing guard. | No | agent |
| D055 | M009 | data | Coins stored in MetaSaveData | Add `int coins` field to MetaSaveData — same JSON blob, same PlayerPrefsMetaSaveService | Consistent with golden pieces pattern; one save blob; JsonUtility handles new fields gracefully (defaults to 0 on old saves) | No | agent |
| D056 | M009 | scope | Overlay HUD sort order 120 | UnityCurrencyOverlay Canvas at sort order 120 — above blocker (100), below bottom popup (150+ when stacking active) but above non-stacked popups (50) | Contextual overlay must be visible when no stacking is happening but hidden behind stacked content when stacking is active. Sort 120 achieves this. | Yes — adjust if sort order scheme changes | agent |
| D057 | M010 | arch | simple-jigsaw as git submodule at Packages/simple-jigsaw/ | Git submodule (not a package tarball or direct copy) — editable and pushable | Library is actively developed alongside the game; submodule allows iterating on the package from within the project and pushing upstream | No | agent |
| D058 | M010 | arch | Local UPM path points to Assets/SimpleJigsaw/ subdirectory | manifest.json entry: `"file:simple-jigsaw/Assets/SimpleJigsaw"` — not repo root | package.json lives at Assets/SimpleJigsaw/package.json in the source repo; Unity local path must point to the directory containing package.json | No | agent |
| D059 | M010 | arch | URP added to manifest alongside built-in RP | Add com.unity.render-pipelines.universal to Packages/manifest.json; do not set URP asset as GraphicsSettings default | simple-jigsaw shaders require URP; existing game scenes use built-in RP and are unaffected as long as no URP pipeline asset is set as the project default renderer | Yes — migrate existing scenes to URP in a future milestone if needed | agent |
| D060 | M010 | scope | JigsawDemo scene not added to EditorBuildSettings | Standalone dev scene only — not in build settings, not wired to game flow | Demo is a development verification tool, not a shipping screen; keeping it out of build settings avoids accidentally including it in player builds | Yes — add to build settings when demo becomes a real game scene | agent |
| D061 | M011 | arch | Puzzle domain in dedicated pure-C# assembly (SimpleGame.Puzzle) | New `SimpleGame.Puzzle` asmdef with `noEngineReferences: true`; no reference to `SimpleJigsaw` | Domain model must survive jigsaw package changes; zero Unity deps enables fast EditMode testing; isolation is the point of the milestone | No — foundational for puzzle domain | agent |
| D062 | M011 | arch | Jigsaw adapter is the only SimpleJigsaw coupling point | `JigsawLevelFactory` in `SimpleGame.Game` is the sole file allowed to import `SimpleJigsaw.*` types | Package is explicitly expected to change; a single adapter class is the only thing that needs updating when it does | No — required coupling boundary | agent |
| D063 | M011 | pattern | Tap handler is a thin forwarder MonoBehaviour only | `PieceTapHandler : MonoBehaviour` has one job: fire `IInGameView.OnTapPiece(pieceId)` on click; no game logic, no service refs | Logic distributed across piece GameObjects is explicitly rejected by user; all game rules live in PuzzleSession | No — foundational for piece interaction model | agent |
| D064 | M011 | pattern | Seeds pre-placed on board construction | Seed piece IDs are part of `IPuzzleLevel`; `PuzzleSession` places them on the board before the first deck draw | Keeps session startup deterministic; seeds are level design data, not runtime choices | Yes — revisit if runtime seed placement is needed for a mechanic | agent |
| D065 | M011 | scope | Single shared deck (one slot) for first playable level | M011 demo uses one shared deck; multi-slot is modelled but not exercised | Simplest path to a playable level; deck model supports multi-slot for future level design | Yes — activate multi-slot when level design requires it | agent |
| D066 | M012 | arch | PuzzleModel replaces PuzzleSession entirely | PuzzleSession, IPuzzleLevel, and PuzzleLevel are deleted; PuzzleModel is the sole domain object | PuzzleSession's implicit tray-window design is the root cause of sync bugs; clean replacement rather than coexistence avoids dual-maintenance | No — foundational replacement decision | agent |
| D067 | M012 | arch | Slots are first-class tracked state in PuzzleModel | PuzzleModel owns an array of N slot entries (each holding a piece ID or null); slots are not inferred from a deck cursor at render time | View sync bugs originate from the presenter inferring slot contents from deck position; explicit slot state eliminates the inference | No — core design of the new model | agent |
| D068 | M012 | arch | Single shared deck, N independent slots | One ordered deck; each slot draws independently from the deck top on correct placement | User explicitly chose this mechanic — slots share one deck pool and refill independently | No — mechanic decision | agent |
| D069 | M012 | pattern | Model fires typed events; presenter subscribes reactively | PuzzleModel exposes OnSlotChanged, OnPiecePlaced, OnCompleted; InGamePresenter subscribes in Initialize() | Consistent with D028; eliminates polling/diffing in presenter; event boundary is the natural separation between pure domain and Unity presenter | No — extends established D028 pattern to PuzzleModel | agent |
| D070 | M012 | data | Slot count configured via PuzzleModelConfig ScriptableObject | PuzzleModelConfig SO with SlotCount field; InGameSceneController reads it and passes to PuzzleModel constructor | User chose SO over serialized field — enables per-level config without scene changes | Yes — extend with other model params if needed | agent |
| D071 | M012 | pattern | Wrong tap: slot piece unchanged, heart deducted | On rejected placement attempt, slot is not refilled; piece stays in slot; heart is consumed by presenter | User explicitly chose this mechanic — same piece stays so player can place it later once a neighbour is placed | No — mechanic decision | agent |
| D072 | M012 | arch | IInGameView moves to slot-indexed API | RefreshTray(int?[]) replaced by RefreshSlot(int slotIndex, int? pieceId) | Slot-indexed updates make it impossible for the view to receive stale window state; each call targets exactly one slot | No — supersedes the window-based tray API | agent |
| D073 | M013 | arch | SolvableShuffle in SimpleGame.Puzzle, not SimpleGame.Game | Standalone class in the pure-C# domain assembly | Algorithm only needs IPuzzlePiece (neighbor data) — no Unity deps; placing it in Puzzle assembly makes it independently testable and reusable without the game layer | No | agent |
| D074 | M013 | pattern | Anti-trivialisation via candidate shuffling + controlled invalid placement | Fisher-Yates shuffle candidates first, then allow an invalid pick at position i if a valid pick exists within slotCount-1 positions | Pure random-first ordering avoids always picking easiest piece; controlled invalid placement creates genuinely non-trivial windows without risking deadlock | Yes — revisit if difficulty tuning needs more control | agent |
| D075 | M013 | pattern | IsSolvable retained as post-hoc safety net, retry cap 10 | Keep existing IsSolvable/retry loop in BuildSolvable unchanged except maxAttempts default 100 → 10 | SolvableShuffle makes IsSolvable rarely needed; retaining it catches any edge case bugs in the new algorithm; 10 retries is sufficient headroom | Yes — remove retry loop entirely once SolvableShuffle is proven stable | agent |
| D076 | M015 | arch | Two-tier folder structure: fastlane/ + tools/ | Project-specific lanes in fastlane/; higher-level orchestration in tools/fastlane/; shared Ruby helpers in tools/fastlane/lib/ | User explicitly requires the tools/ layer to be extractable to a separate repo later; the interface between tiers is the lib/ modules only | Yes — tools/ moves out when multi-project scope is activated | agent |
| D077 | M015 | pattern | Dry-run via with_dry_run wrapper, not lane-level flags | Shared `with_dry_run(name, desc, &block)` helper in tools/fastlane/lib/dry_run.rb; lanes call it for every API mutation | Centralized dry-run logic; consistent output format; easy to audit that every action is covered; validation (config file existence, format checks) still runs in dry-run mode | No | agent |
| D078 | M015 | constraint | Android app creation: documented manual step, not automated | Bootstrap lane prints clear instructions for the one-time manual Play Console step and blocks with confirmation; does not attempt to automate what the API cannot do | Google Play API SDK does not support programmatic app creation — confirmed upstream. Honest tooling beats clever workarounds that silently break. | No — hard platform limit | agent |
| D079 | M015 | pattern | Build number written to ProjectSettings.asset before every build | Lane reads bundleVersion + AndroidBundleVersionCode, increments dd counter, writes back before invoking Unity CLI | Follows existing Docs/BUILD_NUMBERS.md convention exactly; stores counter in the existing field rather than a separate tracking file | No — extends established build number convention | agent |
| D080 | M015 | arch | Status lane returns JSON to stdout | fastlane status calls status_ios + status_android sub-lanes; merges results; prints JSON via puts | Machine-readable output is the requirement (R129); JSON is universally parseable by scripts and dashboards | Yes — add structured logging if output schema evolves | agent |
| D081 | M016 | arch | PlayFab SDK install method | Manual `.unitypackage` import into `Assets/PlayFabSDK/` | No UPM package exists for PlayFab Unity SDK (confirmed: no `package.json` in repo, no scoped registry). Manual import is the only supported path. | Yes � if PlayFab publishes a UPM package | agent |
| D082 | M016 | arch | `IMetaSaveService` stays synchronous; cloud sync is a separate async layer | Cloud pull/push are explicit async steps in `GameBootstrapper.Start()` only; service `Save()`/`Load()` remain synchronous | Changing `IMetaSaveService` to async would require updating all callers (`CoinsService`, `GoldenPieceService`, `MetaProgressionService`) � a large breaking change. Isolating cloud sync to boot is correct for a casual game where offline play during a session is always local. | Yes � revisit if real-time cloud sync becomes a requirement | agent |
| D083 | M016 | pattern | Take-max per field as cloud save conflict strategy | `coins = max(local, cloud)`, `goldenPieces = max(local, cloud)`, `objectProgress[i].currentSteps = max(local, cloud)` | All three fields are monotonically increasing in normal gameplay. Take-max is safe, simple, and can never roll back legitimate progress. Last-write-wins risks losing an offline session on device switch. | Yes � revisit if a feature requires subtracting and persisting currency offline | agent |
| D084 | M016 | pattern | PlayFab callback API wrapped with UniTask via TaskCompletionSource adapters | All `PlayFabClientAPI.*` calls are bridged to UniTask using a local `PlayFabCallbackExtensions.ToUniTask<T>()` helper | The existing boot flow is `async UniTaskVoid`; mixing callback-style calls directly into it produces hard-to-follow control flow. A thin adapter is ~20 lines and makes the entire service layer awaitable with no structural changes. | No � established pattern for all PlayFab calls in the project | agent |
| D085 | M016 | arch | Anonymous login via `LoginWithCustomID` using `SystemInfo.deviceUniqueIdentifier` | `CreateAccount: true`; same call on every launch recovers the same account | Device unique identifier is stable across app reinstalls on iOS and Android. It does not require any user interaction. The simplest path that satisfies the anonymous account requirement. | Yes � revisit if device ID instability is reported in practice | agent |
| D086 | M017 | library | Unity Ads SDK | Advertisement Legacy (com.unity.ads 4.12.x) | Only actively maintained Unity-native ad SDK; "Advertisement Legacy" naming is cosmetic — it is the current SDK and supports iOS/Android with rewarded, interstitial, and banner. No mediation layer needed at this scope. | Yes — switch to Unity LevelPlay/IronSource if mediation is added later | agent |
| D087 | M017 | arch | IAdService abstraction over direct Advertisement calls | All game code uses IAdService; UnityAdService wraps SDK; NullAdService used in tests | Keeps game layer testable without SDK; follows the same interface-first pattern as IAnalyticsService, ICloudSaveService, etc. | No | agent |
| D088 | M017 | pattern | UniTaskCompletionSource bridge for ad callbacks | Same pattern as PlayFab callbacks in M016 — SDK callbacks resolve a TCS, caller awaits the task | SDK is callback-based; bridging to UniTask keeps the game loop awaitable without callback soup | No — established pattern for all SDK callback bridges in this project | agent |
| D089 | M017 | arch | Test game IDs hardcoded in UnityAdService behind testMode flag | Unity test IDs: iOS 5314539, Android 5314538; real IDs wired later via remote config or ScriptableObject | Avoids blocking M017 on dashboard setup; test mode ads work without a real Unity Ads account | Yes — replace with production IDs when ready for release | agent |
| D090 | M017 | pattern | Rewarded ad unavailable: deny with UI feedback (button gray + status text), not grant | When IsRewardedLoaded=false: SetWatchInteractable(false) + UpdateStatus message; WaitForResult stays pending until Skip | User explicitly requested deny-with-message over silent grant | No | agent |
| D091 | M017 | pattern | Interstitial unavailable: skip silently | AdResult.NotLoaded or Failed from ShowInterstitialAsync: log only, navigation continues | User requested silent skip; interstitial is supplemental revenue, not a gate | No | agent |
| D092 | M017 | pattern | Interstitial frequency is session-scoped counter | _levelsCompletedThisSession resets on app restart; no cross-session persistence | Out of scope per discussion; simple, no storage overhead | Yes — add cross-session persistence if frequency caps become a requirement | agent |
| D093 | M018 | arch | Consent gate fires before everything else in boot | ConsentGate check is the first thing in GameBootstrapper.Start() — before PlayFab login, before LevelPlay init | ATT must precede any SDK that uses IDFA per Apple guidelines; ToS acceptance is a legal gate that should not be bypassable by any other boot path | No | agent |
| D094 | M018 | pattern | No dismiss path on ConsentGate popup | IConsentGateView exposes only OnAcceptClicked — no OnCloseClicked, no OnSkipClicked, no back-gesture handler | User requirement: player must accept to proceed; popup re-shows every launch until accepted | No | agent |
| D095 | M018 | arch | ATT via com.unity.ads.ios-support package | Use ATTrackingStatusBinding from Unity's iOS 14 Advertising Support package rather than a custom native plugin | Official Unity package; already relevant given LevelPlay ads SDK in use; handles post-build plist injection via companion post-build script | Yes — swap if package becomes unmaintained | agent |
| D096 | M018 | pattern | IATTService abstraction with NullATTService for non-iOS | IATTService interface + UnityATTService (#if UNITY_IOS) + NullATTService (Editor/Android) | Consistent with IAdService, IAnalyticsService pattern; keeps game layer testable; ATT code compiled out on Android | No | agent |
| D097 | M018 | arch | LevelPlay init deferred until after ATT result | UnityAdService.Initialize() called after awaiting IATTService.RequestAuthorizationAsync() | Apple requires ATT before any SDK IDFA access; initializing before ATT means IDFA is always zeroed even for willing users | No | agent |
| D095b | M018/S02 | arch | ATT via direct P/Invoke bridge (supersedes D095) | ATTBridge.mm native plugin instead of com.unity.ads.ios-support package | ios-support 1.2.0 tgz was partially downloaded; 1.0.0 caused Unity MCP to become unresponsive after import (background SKAdNetwork network fetcher blocked domain reload reconnect). Direct P/Invoke is simpler — two extern C functions, zero package dependencies, identical ATT behaviour | No | agent |
| D098 | M019 | arch | IIAPService abstraction over Unity Purchasing | IIAPService interface + MockIAPService (Editor) + UnityIAPService (device) | Consistent with IAdService, IAnalyticsService, IATTService pattern; keeps game layer testable without store SDK; mock covers all four outcomes via ScriptableObject config | No | agent |
| D099 | M019 | pattern | ConfirmPendingPurchase only after PlayFab validation | UnityIAPService calls ConfirmPendingPurchase on IStoreController only after PlayFab ValidateIOSReceipt / ValidateGooglePlayPurchase succeeds | Prevents receipt loss if PlayFab rejects; Unity Purchasing will re-deliver the pending purchase on next launch | No | agent |
| D100 | M019 | arch | IAPProductCatalog ScriptableObject as single source of truth for product IDs | IAPProductCatalog asset holds ProductId, CoinsAmount, DisplayName for each pack; passed to both ShopPresenter and IAPPurchasePresenter; product IDs must match PlayFab catalog and store listings exactly | Single point of maintenance; mismatch between presenters and PlayFab catalog was the main drift risk with the old stub approach | No | agent |
| D101 | M019 | pattern | IAPPurchase popup grants coins, not golden pieces | IAPPurchasePresenter rewritten to call IIAPService.BuyAsync; goldenPiecesGranted parameter removed; coins granted via IIAPService internally | The stub was wrong — golden pieces are earned through gameplay only; the popup is an IAP surface for coins | No | agent |
| D102 | M019 | arch | Use PlayFab classic Client API validate endpoints (not Economy v2) | ValidateIOSReceipt + ValidateGooglePlayPurchase in PlayFab.ClientModels | Economy v2 / Catalog v2 requires additional setup and a different SDK surface; classic endpoints are already present in the installed PlayFab SDK and sufficient for this scope | Yes — revisit if Economy v2 becomes required | agent |
| D109 | M022 | arch | UGUI HorizontalLayoutGroup deck panel replaces invisible overlay buttons | InGameView owns a _pieceButtonContainer with HorizontalLayoutGroup; buttons created by SetupDeckPanel(); PuzzleStageController.SpawnSlotButtons() and SlotButtonCanvas deleted | Overlay buttons positioned every LateUpdate are fragile; UGUI layout group is the correct Unity pattern for a row of tap targets | No | agent |
| D110 | M022 | pattern | SetupDeckPanel is a concrete method on InGameView, not on IInGameView | PuzzleStageController holds InGameView directly (SerializeField), so it can call SetupDeckPanel() without changing the IInGameView interface | Avoids touching IInGameView and all its mocks (K004); stage controller already holds a concrete InGameView ref, not the interface | No | agent |
| D106 | M021 | arch | Scene controllers are wiring boards only | Scene controllers hold `[SerializeField]` fields + `Initialize()` delegation + `RunAsync()` delegation — no business logic inline | User requirement: controllers should use composition; presenters do the work | No | agent |
| D107 | M021 | arch | PuzzleStageController extracts all 3D logic from InGameSceneController | New MonoBehaviour `PuzzleStageController` owns SpawnPieces, SpawnSlotButtons, LateUpdate tray layout, MovePieceToTraySlot, ShakePieceInSlot, RevealPiece, piece tracking dictionaries | Must be a MonoBehaviour — owns LateUpdate and scene GameObject references; this is the correct Unity component role | No | agent |
| D108 | M021 | arch | InGameFlowPresenter extracts gameplay loop from InGameSceneController | New pure C# class `InGameFlowPresenter` owns RunAsync game loop, popup orchestration (LevelComplete, LevelFailed, Shop, RewardedAd, Interstitial), retry flow, analytics calls | Pure C# — no MonoBehaviour, no Unity lifecycle; follows D026 presenter pattern | No | agent |
| D105 | M020 | convention | Namespaces unchanged during folder restructure | SimpleGame.Game.Services and SimpleGame.Game.Popup namespaces kept as-is despite folder moves | Changing namespaces requires updating scene/asset m_EditorClassIdentifier strings, all using statements, and test files — cost with no runtime benefit; folders communicate intent, namespaces can follow in a future dedicated pass if desired | Yes — revisit namespace alignment if IDE navigation or namespace-based tooling becomes a pain point | agent |
| D111 | M023 | pattern | Level start camera sequence | Full board overview first, then smooth zoom to first valid placement area | Gives the player spatial context before gameplay begins; establishes board layout awareness before zooming into the action area. | Yes — if it feels too slow in practice, can be shortened or skipped | human |
