# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-15 | S01/T01 | UniTask installation method | Git URL (`https://github.com/Cysharp/UniTask.git?path=src/UniTask/Assets/Plugins/UniTask`) added directly to `Packages/manifest.json` | No OpenUPM registry available in batchmode; git URL is the canonical documented approach and resolves at first project open | Yes — switch to OpenUPM scoped registry if network reliability becomes an issue |
| 2 | 2026-03-15 | S01/T01 | Assembly reference format in `.asmdef` | Used string name references (`"UniTask"`, `"SimpleGame.Runtime"`) rather than GUID references | Name references are portable and readable; GUID references require the asset database to be populated which isn't guaranteed in CLI-only workflows | No — Unity recommends GUIDs for robustness but names work for this project scope |
| 3 | 2026-03-15 | S01/T02 | View interface event convention | `event Action` (plain C# delegate) not `UnityEvent` or `Button.onClick` | Keeps all view interfaces free of Unity types; enables pure C# mock implementations in edit-mode tests with no Unity runtime dependency | No — this is the foundational convention for all view interfaces in the project |
| 4 | 2026-03-15 | S01/T02 | Two-phase presenter lifecycle (ctor + Initialize) | Constructor only sets fields; `Initialize()` performs event subscription and view setup | Allows async initialization in S02 without changing the constructor signature; separates wiring (Initialize) from injection (constructor) | No — downstream slices build on this lifecycle pattern |
| 5 | 2026-03-15 | S01/T02 | `GameService` placed in `SimpleGame.Core.Services` namespace | Separate `Core/Services/` folder from `Core/MVP/` | Enforces separation of domain logic from MVP infrastructure; domain services can be unit-tested and reused independently of presenter hierarchy | No — establishes the namespace convention for all domain services |
| 6 | 2026-03-15 | S01/T03 | NUnit test framework installation | Added `com.unity.test-framework` to `Packages/manifest.json` (resolved to 1.6.0) | The package was absent from the project; NUnit is delivered via `com.unity.ext.nunit` as a transitive dependency of the test framework — without it `NUnit.Framework` types can't be found at compile time | No — required for all edit-mode and play-mode tests in this project |
| 7 | 2026-03-15 | S01/T03 | Unity `-runTests` batchmode: omit `-quit` flag | Do NOT pass `-quit` alongside `-runTests` | `-quit` races with the async test runner and causes Unity to exit before writing `TestResults.xml`; test runner exits on its own when complete | No — standard usage pattern for Unity batchmode test runs |
| 8 | 2026-03-15 | S01/T03 | MockSampleView disposal test uses call-count tracking | Added `UpdateLabelCallCount` int property to `MockSampleView` | After `Dispose()`, the welcome message label text stays the same if a click fires (same string from `GetWelcomeMessage()`); testing by string equality gives a false pass — call count definitively detects whether `UpdateLabel` was invoked post-Dispose | No — any future mock for `ISampleView` should include a call counter for reliable disposal testing |
| 9 | 2026-03-15 | S02 | ScreenManager does not own presenter lifecycle | ScreenManager only handles scene load/unload; presenter creation/disposal happens in S05 boot flow | Keeps S02 focused on navigation logic (independently testable). Presenter wiring requires view instances found after scene load — mixing concerns here creates lifecycle ordering bugs. S05 connects UIFactory to loaded screen views. | No — ScreenManager is a navigation primitive, not a presenter container |
| 10 | 2026-03-15 | S02 | ScreenId enum over raw strings for screen identification | `ScreenId` enum with `ToSceneName()` helper instead of passing string scene names | Compile-time safety prevents typos, makes the set of screens discoverable, trivial mapping cost. Flagged as an open question in context — resolved in favor of enum. | Yes — add new enum values as screens are added |
| 11 | 2026-03-15 | S02/T01 | ToSceneName helper omitted; enum.ToString() used directly in ScreenManager | `screenId.ToString()` in ScreenManager instead of a static helper on ScreenId | The static-state grep guard (`grep -v "static void|static class|static readonly|static async|static UniTask"`) does not whitelist `static string`, so any static method returning string triggers a false positive. `enum.ToString()` produces identical output for enums whose member names equal scene names. If scene names ever diverge from enum names, reintroduce the helper as a non-static lookup table inside a non-static class. | Yes — revisit if scene names diverge from enum names |
| 12 | 2026-03-15 | S02/T02 | Unity scene creation via batchmode -executeMethod | Static method in `Assets/Editor/` called via `-executeMethod ClassName.MethodName` using `EditorSceneManager.NewScene` + `SaveScene` + `EditorBuildSettings.scenes` assignment | Batchmode -executeMethod is the canonical Unity CLI pattern for editor asset operations; placing the script in `Assets/Editor/` (no asmdef needed) gets it auto-compiled as editor-only via Assembly-CSharp-Editor | No — established pattern for any future editor asset setup scripts |
| 13 | 2026-03-15 | S03/T02 | DismissAllAsync calls Unblock() per popup inside the while loop | `_inputBlocker.Unblock()` is inside the while loop in `DismissAllAsync`, not after it | Reference-counting contract: each ShowPopupAsync increments the block count once; each dismiss must decrement once. A single Unblock() after the loop with 2+ popups on the stack leaves the count positive, making IsBlocked=true after dismiss-all. Moving Unblock() inside the loop ensures balanced increment/decrement regardless of stack depth. | No — required for correct reference-counting behavior |
| 14 | 2026-03-15 | S04/T01 | ITransitionPlayer and IInputBlocker injected as optional constructor params into ScreenManager | Optional params `ITransitionPlayer transitionPlayer = null, IInputBlocker inputBlocker = null` on ScreenManager constructor | Keeps navigation atomic inside the existing `_isNavigating` guard; avoids a "raw vs wrapped" navigation split; all 27 existing `new ScreenManager(loader)` calls compile unchanged without modification | No — downstream S05 wiring builds on this injection pattern |
| 15 | 2026-03-15 | S04/T01 | MergedLogTransitionPlayer/MergedLogSceneLoader helpers for ordering tests | Thin wrappers that share a single `List<string>` log to record interleaved events in arrival order | A single merged log gives precise ordering assertions (fadeOut[0], unload[1], load[2], fadeIn[3]) without fragile index arithmetic across separate logs | No — reusable pattern for any future interleaved-ordering test |
| 16 | 2026-03-15 | S05 | Presenters receive navigation/popup callbacks, not full manager references | Presenters take `Action<ScreenId>`, `Action<PopupId>`, `Func<UniTask>` callbacks instead of ScreenManager/PopupManager | Keeps presenters fully testable in edit-mode with simple lambda mocks; avoids coupling presenters to manager internals; GameBootstrapper creates the closures that delegate to real managers | Yes — could switch to thin navigation/popup interfaces if the callback count grows beyond 2-3 per presenter |
| 17 | 2026-03-15 | S05 | Popup overlay approach: pre-instantiated show/hide, not scene-based or prefab-instantiated | UnityPopupContainer shows/hides a pre-instantiated popup GameObject in Boot scene via SetActive(true/false) | Avoids complexity of registering popup scenes in EditorBuildSettings and lifecycle issues of additive-loading tiny popup scenes; avoids prefab loading concerns; ConfirmDialog lives in Boot scene, starts inactive | Yes — switch to prefab instantiation if popup count grows significantly |
| 18 | 2026-03-15 | S05/T02 | com.unity.ugui added to manifest.json and SimpleGame.Runtime.asmdef | `"com.unity.ugui": "2.0.0"` added to Packages/manifest.json; `"UnityEngine.UI"` added to references array in SimpleGame.Runtime.asmdef | Unity 6 (6000.x) ships uGUI as `com.unity.ugui` built-in package — it is NOT auto-available without being declared. `UnityEngine.UI.Button` and `UnityEngine.UI.Text` types are inaccessible without it. | No — required for all scripts using legacy uGUI (Button, Text, Image, Canvas, etc.) |
| 19 | 2026-03-15 | S05/T02 | SceneSetup moved to SimpleGame.Editor asmdef | Created `Assets/Editor/SimpleGame.Editor.asmdef` referencing SimpleGame.Runtime and UnityEngine.UI | Without an explicit asmdef, editor scripts land in Assembly-CSharp-Editor which cannot reference custom asmdef assemblies. SceneSetup uses Runtime MonoBehaviour types (GameBootstrapper, MainMenuView, etc.) so it needs the asmdef reference. | No — required pattern for any editor script that needs to reference custom runtime assemblies |
| 20 | 2026-03-15 | S05/T02 | GameBootstrapper uses FindFirstObjectByType instead of deprecated FindObjectOfType | All scene-object discovery uses `FindFirstObjectByType<T>()` | Unity 6 deprecates `Object.FindObjectOfType<T>()` with CS0618; `FindFirstObjectByType<T>()` is the replacement with identical semantics for boot-time (single-instance) discovery. Avoids compiler warnings in batchmode logs. | No — use FindFirstObjectByType for all single-instance scene lookups going forward |
| 21 | 2026-03-15 | S05/T02 | SceneSetupHelpers helper class to satisfy static-state grep guard | `CreateFullScreenCanvas` and `CreateButton` helpers extracted to `SceneSetupHelpers` class using `static void` with `out` parameters | The static-state grep guard (`grep -v "static void\|static class\|static readonly\|static async\|static UniTask"`) does not whitelist `static Canvas` or `static GameObject` return types. Converting to `static void` with out-params makes all lines match the filter. This is an editor-only utility; the guard is intended to catch runtime shared mutable state, not factory methods. | Yes — if the guard filter is updated to whitelist `static [TypeName]` for known return types, this can be reverted |
| 22 | 2026-03-16 | M002 | ScreenManager and PopupManager made generic | `ScreenManager<TScreenId> where TScreenId : struct, System.Enum` and `PopupManager<TPopupId> where TPopupId : struct, System.Enum` | Required to remove game-specific `ScreenId`/`PopupId` enum dependencies from Core; `struct` constraint required for `Nullable<T>` support (`.HasValue`/`.Value`); `System.Enum` documents intent; Unity 6 uses C# 9 so both constraints are supported | No — required for Core to be game-agnostic |
| 23 | 2026-03-16 | M002 | Core Unity implementations grouped under `Core/Unity/` subfolder | `UnitySceneLoader`, `UnityInputBlocker`, `UnityTransitionPlayer` moved to `Assets/Scripts/Core/Unity/` subfolders | Distinguishes Unity-specific implementations from pure C# abstractions within Core; clearer than `Core/Runtime/` which implied a Core/Editor split that doesn't exist | Yes — rename to `Core/Runtime/` if convention changes |
| 24 | 2026-03-16 | M002 | `ISampleView`/`SamplePresenter` moved to Core test assembly as fixtures | These types live in `Assets/Tests/EditMode/Core/` under `SimpleGame.Tests.Core.asmdef`, not in runtime sources | They exist solely to exercise the MVP pattern in tests — not used by any runtime code; moving them out of runtime keeps Core runtime sources free of demo/sample code | No — test fixtures belong in test assemblies |
| 25 | 2026-03-16 | M002 | File moves use `git mv` to preserve .meta GUIDs | All `.cs` file moves during M002 use `git mv src dst` rather than raw filesystem copy | Unity scene serialization stores component references by script GUID (in `.meta` files); raw copy creates new GUIDs, silently breaking scene wiring; `git mv` preserves the file identity so Unity resolves the same GUID at the new path | No — mandatory for any file move that affects MonoBehaviours referenced in scenes |
| 26 | 2026-03-16 | M003 | Presenter outbound callback direction | Presenters expose awaitable result methods (`WaitForConfirmation()`, `WaitForAction()`, etc.) instead of receiving outbound callbacks; SceneController awaits them to compose control flow. Supersedes D016. | Callbacks scattered across constructor args make control flow implicit and hard to follow as presenter count grows; awaitable methods make the flow linear and readable in one place (SceneController.RunAsync) | No — established pattern for all presenters going forward |
| 27 | 2026-03-16 | M003 | View→Presenter callbacks remain as event Action | View interfaces continue to expose `event Action` delegates that presenters subscribe to in Initialize(); this direction of callback is correct and unchanged | Events from view to presenter are the right model — views signal user intent without knowing who listens; only the outbound direction (presenter→outside) is replaced by awaitables | No — foundational convention, complements D003 |
| 28 | 2026-03-16 | M003 | Service/Model→Presenter reactive callbacks are permitted | Presenters may subscribe to events or callbacks from domain services/models to react to state changes pushed from the model layer | Services broadcasting state changes (score updates, timer ticks, data loaded) are a legitimate reactive pattern; the ban on callbacks applies only to the presenter→SceneController direction | Yes — revisit if reactive complexity warrants Rx or a dedicated event bus |
| 29 | 2026-03-16 | M004 | Context passing via shared service, not RunAsync params | GameSessionService holds current game context (level ID, score, outcome); scene controllers read from it; ISceneController.RunAsync() signature stays clean | User explicitly chose service-mediated context. Keeps ISceneController interface generic. Receiver scenes are responsible for reading what they need. | No — established pattern for scene-to-scene context |
| 30 | 2026-03-16 | M004 | Distinct popups for distinct dialogs | WinDialog and LoseDialog are new PopupId entries with their own views/presenters; ConfirmDialog stays for generic confirmations | User explicitly said: "Use new popups for clearly different dialogs. Win/lose will look very different. Reuse only for very basic confirmations." | No — design principle for popup types |
| 31 | 2026-03-16 | M004 | In-memory progression only | ProgressionService tracks level in memory. No PlayerPrefs, no JSON save. Resets on app restart. | User explicitly deferred persistence. Keeps service layer clean. Persistence is a future milestone. | Yes — add persistence when R041 is activated |
| 32 | 2026-03-16 | M004 | Play-from-editor fallback via serialized field | InGame scene controller has a serialized default level ID field. When Boot isn't present (direct play from editor), uses this fallback instead of reading from GameSessionService. | User said "keep it simple for now." Avoids editor tooling complexity while supporting dev workflow. | Yes — could be replaced by editor tool or ScriptableObject config |
| D033 | M005 | arch | Transition visuals owned by prefab | Self-contained prefab with Canvas + CanvasGroup + UnityTransitionPlayer MonoBehaviour | User wants future transitions to use images/animations/shaders — prefab must own all visual details. Swapping the prefab changes the look without code changes. | No — foundational for extensible transitions |
| D034 | M005 | library | LitMotion for transition tweening | LitMotion `BindToAlpha`/`ToUniTask()` replaces manual while-loop alpha interpolation | LitMotion already installed as project tweening library; provides correct easing, cancellation, and CanvasGroup binding out of the box | No — LitMotion is the project's tweening standard |
| D035 | M006 | data | ScriptableObjects for meta world data | WorldData → EnvironmentData → RestorableObjectData as ScriptableObjects in Assets/Data/ | Static game data that designers edit — SO is the Unity-native pattern; flat structure with blocked-by lists per user request, not a tree | Yes — could migrate to JSON/Addressables if data volume requires it |
| D036 | M006 | arch | Meta persistence via interface-backed PlayerPrefs | IMetaSaveService interface with PlayerPrefsMetaSaveService implementation using JSON serialization | User wants persistence now but implementation swappable later (file, cloud); PlayerPrefs is simplest for dev; JSON handles structured per-object progress | Yes — swap to file/cloud when needed |
| D037 | M006 | arch | Main screen IS the meta world | No separate MetaWorld screen — MainMenu scene shows environment, objects, balance, play button, settings | User explicitly said "the meta world will be in the main screen"; reduces screen count, puts spending right next to playing | No — core UX decision |
| D038 | M006 | pattern | Tap-to-spend without confirmation | Tapping an unblocked object spends one golden piece for one restoration step — no confirmation popup | User said "no confirmation on tap to spend" — UX will change later, keep it simple now | Yes — add confirmation or animation later |
| D039 | M006 | scope | Stub gameplay: counter + correct/incorrect buttons only | Core gameplay is level ID, piece counter, place-correct/place-incorrect, hearts — no board, tray, or neighbor logic | User explicitly said core gameplay comes later; this milestone proves the flow skeleton | Yes — replaced by real gameplay in a future milestone |
| D040 | M006 | scope | Ad/IAP as tappable UI stubs, not just interfaces | RewardedAd and IAPPurchase get full popup UIs with text stubs, not just C# interfaces | User explicitly said "UI stubs, not only interface stubs" | Yes — replaced by real SDK integration later |
| D041 | M007 | arch | View resolution via IViewResolver in Core | Generic `IViewResolver` with `Get<T>() where T : class` — separate from IPopupContainer | User wants generic resolution on a separate interface. Keeps Core game-agnostic. Container implements both IPopupContainer and IViewResolver. | No |
| D042 | M007 | arch | Popup container renamed to reflect expanded role | UnityPopupContainer renamed (e.g. ViewContainer) to signal it does more than show/hide | User explicitly chose rename over keeping old name | No |
| D043 | M007 | arch | Scene root convention for controller resolution | After additive scene load, find scene controllers by querying loaded scene's root GameObjects — not FindFirstObjectByType | User chose scene root convention over self-registration pattern | No |
| D044 | M007 | arch | GameBootstrapper uses SerializeField refs for boot infrastructure | UnityInputBlocker, UnityTransitionPlayer, container wired via [SerializeField] on GameBootstrapper | User chose explicit refs over scene scanning. All components are in Boot scene — straightforward. | No |
| D045 | M007 | scope | Popup views only — screen views stay in their scenes | Only popup views move to prefab-based container. Screen views (MainMenu, Settings, InGame) remain in their scenes with SerializeField refs. | User explicitly chose popups only for now — screens are a separate concern. | Yes — extend to screens in a future milestone if needed |
| D046 | M007 | arch | Popup container holds inactive children (not instantiation) | Container holds popup prefab instances as inactive children, activated via SetActive — not instantiated on demand | User mentioned instantiation as a future possibility. Inactive children is the stepping stone. Supersedes D017 (pre-instantiated show/hide) only in organization, not mechanism. | Yes — switch to instantiation when popup count grows (R078) |
| D047 | M007/S02 | arch | IViewResolver parameter on scene controller Initialize() is optional (= null default) | Added as optional last parameter `IViewResolver viewResolver = null` on both InGameSceneController.Initialize() and MainMenuSceneController.Initialize() | Preserves backward-compat for all existing call sites; makes null-passing safe in tests where SetViewsForTesting() overrides take precedence; avoids CS7036 errors without requiring every call site updated simultaneously | Yes — make required once S03 ensures all call sites pass a resolver |
| D048 | M007/S02 | pattern | View getter resolution order in scene controllers | Resolution order: override field (SetViewsForTesting test seam) → SerializeField ref (if present) → _viewResolver?.Get<T>() → Debug.LogError + return null | Test seam must take precedence over production resolver to keep tests isolated. SerializeField ref checked before resolver for legacy scene-placed support. Resolver is the general fallback. LogError preserves failure visibility when all sources return null. | No — established resolution order for all scene controller view getters |
| D049 | M008 | arch | Animation layer: PopupViewBase as abstract MonoBehaviour with default LitMotion impls | Abstract base class PopupViewBase : MonoBehaviour with virtual AnimateInAsync/AnimateOutAsync. Defaults: bounce-up in (Y offset + OutBounce), scale+fade out (scale 0.85 + alpha 0, InBack). All popup views inherit it. | User wants overrideable defaults without forcing every view to implement from scratch. Abstract class fits Unity MonoBehaviour constraint; interface alone can't carry default impls in C# at this Unity version. | No |
| D050 | M008 | arch | Blocker fade: single layer repositions, input unblocks at fade-out start | Single UnityInputBlocker instance fades in/out. IInputBlocker gains FadeInAsync/FadeOutAsync. PopupManager calls Block()+FadeInAsync concurrent with ShowPopupAsync, then calls Unblock()+starts FadeOutAsync before awaiting HidePopupAsync. | User confirmed single repositioning layer. Input timing split is a product requirement (R080) — unblock must happen before animation ends. | No |
| D051 | M008 | library | TMP for all new prefab UI components | TextMeshPro for all prefab text and button labels. Existing non-prefab uGUI Text stays until SceneSetup rewrite replaces it. | User explicitly chose TMP. Better typography, standard for mobile. Requires adding TMP to Game and Editor asmdefs. | No |
| D052 | M009 | arch | In-scene screen manager: SetActive panel swap | `InSceneScreenManager<TScreenId>` uses `SetActive(true/false)` on pre-existing panel GameObjects — no scene loading | User explicitly chose instant swap; no animation this milestone; mirrors ScreenManager<T> generic pattern but avoids scene loading overhead | Yes — add animation in a future milestone |
| D053 | M009 | arch | Popup sort order scheme for stacking | Each popup gets its own Canvas with OverrideSorting. Sort order = 50 + (stackDepth * 100). Blocker at 100. Bottom popup at 50 (below blocker), top popup at 150+ (above blocker). | User requirement: bottom popup must be visually dimmed by overlay when a second popup stacks on top. Per-popup canvas is the only clean Unity mechanism. | No |
| D054 | M009 | arch | PopupManager _isOperating guard: keep as-is | The guard blocks *concurrent* re-entrant calls only. Sequential stacking (show B after A's WaitForChoice returns) does not hit the guard. No change needed. | Removing the guard entirely would allow unintended parallel operations. Sequential stacking works naturally with the existing guard. | No |
| D055 | M009 | data | Coins stored in MetaSaveData | Add `int coins` field to MetaSaveData — same JSON blob, same PlayerPrefsMetaSaveService | Consistent with golden pieces pattern; one save blob; JsonUtility handles new fields gracefully (defaults to 0 on old saves) | No |
| D056 | M009 | scope | Overlay HUD sort order 120 | UnityCurrencyOverlay Canvas at sort order 120 — above blocker (100), below bottom popup (150+ when stacking active) but above non-stacked popups (50) | Contextual overlay must be visible when no stacking is happening but hidden behind stacked content when stacking is active. Sort 120 achieves this. | Yes — adjust if sort order scheme changes |
| D057 | M010 | arch | simple-jigsaw as git submodule at Packages/simple-jigsaw/ | Git submodule (not a package tarball or direct copy) — editable and pushable | Library is actively developed alongside the game; submodule allows iterating on the package from within the project and pushing upstream | No |
| D058 | M010 | arch | Local UPM path points to Assets/SimpleJigsaw/ subdirectory | manifest.json entry: `"file:simple-jigsaw/Assets/SimpleJigsaw"` — not repo root | package.json lives at Assets/SimpleJigsaw/package.json in the source repo; Unity local path must point to the directory containing package.json | No |
| D059 | M010 | arch | URP added to manifest alongside built-in RP | Add com.unity.render-pipelines.universal to Packages/manifest.json; do not set URP asset as GraphicsSettings default | simple-jigsaw shaders require URP; existing game scenes use built-in RP and are unaffected as long as no URP pipeline asset is set as the project default renderer | Yes — migrate existing scenes to URP in a future milestone if needed |
| D060 | M010 | scope | JigsawDemo scene not added to EditorBuildSettings | Standalone dev scene only — not in build settings, not wired to game flow | Demo is a development verification tool, not a shipping screen; keeping it out of build settings avoids accidentally including it in player builds | Yes — add to build settings when demo becomes a real game scene |
| D061 | M011 | arch | Puzzle domain in dedicated pure-C# assembly (SimpleGame.Puzzle) | New `SimpleGame.Puzzle` asmdef with `noEngineReferences: true`; no reference to `SimpleJigsaw` | Domain model must survive jigsaw package changes; zero Unity deps enables fast EditMode testing; isolation is the point of the milestone | No — foundational for puzzle domain |
| D062 | M011 | arch | Jigsaw adapter is the only SimpleJigsaw coupling point | `JigsawLevelFactory` in `SimpleGame.Game` is the sole file allowed to import `SimpleJigsaw.*` types | Package is explicitly expected to change; a single adapter class is the only thing that needs updating when it does | No — required coupling boundary |
| D063 | M011 | pattern | Tap handler is a thin forwarder MonoBehaviour only | `PieceTapHandler : MonoBehaviour` has one job: fire `IInGameView.OnTapPiece(pieceId)` on click; no game logic, no service refs | Logic distributed across piece GameObjects is explicitly rejected by user; all game rules live in PuzzleSession | No — foundational for piece interaction model |
| D064 | M011 | pattern | Seeds pre-placed on board construction | Seed piece IDs are part of `IPuzzleLevel`; `PuzzleSession` places them on the board before the first deck draw | Keeps session startup deterministic; seeds are level design data, not runtime choices | Yes — revisit if runtime seed placement is needed for a mechanic |
| D065 | M011 | scope | Single shared deck (one slot) for first playable level | M011 demo uses one shared deck; multi-slot is modelled but not exercised | Simplest path to a playable level; deck model supports multi-slot for future level design | Yes — activate multi-slot when level design requires it |
| D066 | M012 | arch | PuzzleModel replaces PuzzleSession entirely | PuzzleSession, IPuzzleLevel, and PuzzleLevel are deleted; PuzzleModel is the sole domain object | PuzzleSession's implicit tray-window design is the root cause of sync bugs; clean replacement rather than coexistence avoids dual-maintenance | No — foundational replacement decision |
| D067 | M012 | arch | Slots are first-class tracked state in PuzzleModel | PuzzleModel owns an array of N slot entries (each holding a piece ID or null); slots are not inferred from a deck cursor at render time | View sync bugs originate from the presenter inferring slot contents from deck position; explicit slot state eliminates the inference | No — core design of the new model |
| D068 | M012 | arch | Single shared deck, N independent slots | One ordered deck; each slot draws independently from the deck top on correct placement | User explicitly chose this mechanic — slots share one deck pool and refill independently | No — mechanic decision |
| D069 | M012 | pattern | Model fires typed events; presenter subscribes reactively | PuzzleModel exposes OnSlotChanged, OnPiecePlaced, OnCompleted; InGamePresenter subscribes in Initialize() | Consistent with D028; eliminates polling/diffing in presenter; event boundary is the natural separation between pure domain and Unity presenter | No — extends established D028 pattern to PuzzleModel |
| D070 | M012 | data | Slot count configured via PuzzleModelConfig ScriptableObject | PuzzleModelConfig SO with SlotCount field; InGameSceneController reads it and passes to PuzzleModel constructor | User chose SO over serialized field — enables per-level config without scene changes | Yes — extend with other model params if needed |
| D071 | M012 | pattern | Wrong tap: slot piece unchanged, heart deducted | On rejected placement attempt, slot is not refilled; piece stays in slot; heart is consumed by presenter | User explicitly chose this mechanic — same piece stays so player can place it later once a neighbour is placed | No — mechanic decision |
| D072 | M012 | arch | IInGameView moves to slot-indexed API | RefreshTray(int?[]) replaced by RefreshSlot(int slotIndex, int? pieceId) | Slot-indexed updates make it impossible for the view to receive stale window state; each call targets exactly one slot | No — supersedes the window-based tray API
| D073 | M013 | arch | SolvableShuffle in SimpleGame.Puzzle, not SimpleGame.Game | Standalone class in the pure-C# domain assembly | Algorithm only needs IPuzzlePiece (neighbor data) — no Unity deps; placing it in Puzzle assembly makes it independently testable and reusable without the game layer | No |
| D074 | M013 | pattern | Anti-trivialisation via candidate shuffling + controlled invalid placement | Fisher-Yates shuffle candidates first, then allow an invalid pick at position i if a valid pick exists within slotCount-1 positions | Pure random-first ordering avoids always picking easiest piece; controlled invalid placement creates genuinely non-trivial windows without risking deadlock | Yes — revisit if difficulty tuning needs more control |
| D075 | M013 | pattern | IsSolvable retained as post-hoc safety net, retry cap 10 | Keep existing IsSolvable/retry loop in BuildSolvable unchanged except maxAttempts default 100 → 10 | SolvableShuffle makes IsSolvable rarely needed; retaining it catches any edge case bugs in the new algorithm; 10 retries is sufficient headroom | Yes — remove retry loop entirely once SolvableShuffle is proven stable |
| D076 | M015 | arch | Two-tier folder structure: fastlane/ + tools/ | Project-specific lanes in fastlane/; higher-level orchestration in tools/fastlane/; shared Ruby helpers in tools/fastlane/lib/ | User explicitly requires the tools/ layer to be extractable to a separate repo later; the interface between tiers is the lib/ modules only | Yes — tools/ moves out when multi-project scope is activated |
| D077 | M015 | pattern | Dry-run via with_dry_run wrapper, not lane-level flags | Shared `with_dry_run(name, desc, &block)` helper in tools/fastlane/lib/dry_run.rb; lanes call it for every API mutation | Centralized dry-run logic; consistent output format; easy to audit that every action is covered; validation (config file existence, format checks) still runs in dry-run mode | No |
| D078 | M015 | constraint | Android app creation: documented manual step, not automated | Bootstrap lane prints clear instructions for the one-time manual Play Console step and blocks with confirmation; does not attempt to automate what the API cannot do | Google Play API SDK does not support programmatic app creation — confirmed upstream. Honest tooling beats clever workarounds that silently break. | No — hard platform limit |
| D079 | M015 | pattern | Build number written to ProjectSettings.asset before every build | Lane reads bundleVersion + AndroidBundleVersionCode, increments dd counter, writes back before invoking Unity CLI | Follows existing Docs/BUILD_NUMBERS.md convention exactly; stores counter in the existing field rather than a separate tracking file | No — extends established build number convention |
| D080 | M015 | arch | Status lane returns JSON to stdout | fastlane status calls status_ios + status_android sub-lanes; merges results; prints JSON via puts | Machine-readable output is the requirement (R129); JSON is universally parseable by scripts and dashboards | Yes — add structured logging if output schema evolves |
| D081 | M016 | arch | PlayFab SDK install method | Manual `.unitypackage` import into `Assets/PlayFabSDK/` | No UPM package exists for PlayFab Unity SDK (confirmed: no `package.json` in repo, no scoped registry). Manual import is the only supported path. | Yes � if PlayFab publishes a UPM package |
| D082 | M016 | arch | `IMetaSaveService` stays synchronous; cloud sync is a separate async layer | Cloud pull/push are explicit async steps in `GameBootstrapper.Start()` only; service `Save()`/`Load()` remain synchronous | Changing `IMetaSaveService` to async would require updating all callers (`CoinsService`, `GoldenPieceService`, `MetaProgressionService`) � a large breaking change. Isolating cloud sync to boot is correct for a casual game where offline play during a session is always local. | Yes � revisit if real-time cloud sync becomes a requirement |
| D083 | M016 | pattern | Take-max per field as cloud save conflict strategy | `coins = max(local, cloud)`, `goldenPieces = max(local, cloud)`, `objectProgress[i].currentSteps = max(local, cloud)` | All three fields are monotonically increasing in normal gameplay. Take-max is safe, simple, and can never roll back legitimate progress. Last-write-wins risks losing an offline session on device switch. | Yes � revisit if a feature requires subtracting and persisting currency offline |
| D084 | M016 | pattern | PlayFab callback API wrapped with UniTask via TaskCompletionSource adapters | All `PlayFabClientAPI.*` calls are bridged to UniTask using a local `PlayFabCallbackExtensions.ToUniTask<T>()` helper | The existing boot flow is `async UniTaskVoid`; mixing callback-style calls directly into it produces hard-to-follow control flow. A thin adapter is ~20 lines and makes the entire service layer awaitable with no structural changes. | No � established pattern for all PlayFab calls in the project |
| D085 | M016 | arch | Anonymous login via `LoginWithCustomID` using `SystemInfo.deviceUniqueIdentifier` | `CreateAccount: true`; same call on every launch recovers the same account | Device unique identifier is stable across app reinstalls on iOS and Android. It does not require any user interaction. The simplest path that satisfies the anonymous account requirement. | Yes � revisit if device ID instability is reported in practice |

| D086 | M017 | library | Unity Ads SDK | Advertisement Legacy (com.unity.ads 4.12.x) | Only actively maintained Unity-native ad SDK; "Advertisement Legacy" naming is cosmetic — it is the current SDK and supports iOS/Android with rewarded, interstitial, and banner. No mediation layer needed at this scope. | Yes — switch to Unity LevelPlay/IronSource if mediation is added later |
| D087 | M017 | arch | IAdService abstraction over direct Advertisement calls | All game code uses IAdService; UnityAdService wraps SDK; NullAdService used in tests | Keeps game layer testable without SDK; follows the same interface-first pattern as IAnalyticsService, ICloudSaveService, etc. | No |
| D088 | M017 | pattern | UniTaskCompletionSource bridge for ad callbacks | Same pattern as PlayFab callbacks in M016 — SDK callbacks resolve a TCS, caller awaits the task | SDK is callback-based; bridging to UniTask keeps the game loop awaitable without callback soup | No — established pattern for all SDK callback bridges in this project |
| D089 | M017 | arch | Test game IDs hardcoded in UnityAdService behind testMode flag | Unity test IDs: iOS 5314539, Android 5314538; real IDs wired later via remote config or ScriptableObject | Avoids blocking M017 on dashboard setup; test mode ads work without a real Unity Ads account | Yes — replace with production IDs when ready for release |
| D090 | M017 | pattern | Rewarded ad unavailable: deny with UI feedback (button gray + status text), not grant | When IsRewardedLoaded=false: SetWatchInteractable(false) + UpdateStatus message; WaitForResult stays pending until Skip | User explicitly requested deny-with-message over silent grant | No |
| D091 | M017 | pattern | Interstitial unavailable: skip silently | AdResult.NotLoaded or Failed from ShowInterstitialAsync: log only, navigation continues | User requested silent skip; interstitial is supplemental revenue, not a gate | No |
| D092 | M017 | pattern | Interstitial frequency is session-scoped counter | _levelsCompletedThisSession resets on app restart; no cross-session persistence | Out of scope per discussion; simple, no storage overhead | Yes — add cross-session persistence if frequency caps become a requirement |

---

### D093 — SDK: LevelPlay (com.unity.services.levelplay) replaces Advertisement Legacy
**Date:** 2026-03-20
**Scope:** library

**Decision:** Which Unity ad SDK to use

**Choice:** Unity LevelPlay (com.unity.services.levelplay) behind LEVELPLAY_ENABLED compile guard

**Rationale:** com.unity.ads (Advertisement Legacy) failed to resolve from the registry and is deprecated (no further feature updates planned). Unity's current recommendation is LevelPlay (Ads Mediation package, backed by ironSource). UnityAdService is rewritten for the LevelPlay API but compiled out with `#if LEVELPLAY_ENABLED` until the package is installed and an App Key is available — project stays compilable in the interim. IAdService abstraction means no other game code changes.

**To activate:** Install `com.unity.services.levelplay` via Package Manager, install Unity Ads adapter via Ads Mediation → Integration Manager, create LevelPlay app for App Key, add `LEVELPLAY_ENABLED` scripting define.

**Revisable:** No
