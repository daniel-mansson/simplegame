# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-15 | S01/T01 | UniTask installation method | Git URL (`https://github.com/Cysharp/UniTask.git?path=src/UniTask/Assets/Plugins/UniTask`) added directly to `Packages/manifest.json` | No OpenUPM registry available in batchmode; git URL is the canonical documented approach and resolves at first project open | Yes — switch to OpenUPM scoped registry if network reliability becomes an issue |
| 2 | 2026-03-15 | S01/T01 | Assembly reference format in `.asmdef` | Used string name references (`"UniTask"`, `"SimpleGame.Runtime"`) rather than GUID references | Name references are portable and readable; GUID references require the asset database to be populated which isn't guaranteed in CLI-only workflows | No — Unity recommends GUIDs for robustness but names work for this project scope |
| 3 | 2026-03-15 | S01/T02 | View interface event convention | `event Action` (plain C# delegate) not `UnityEvent` or `Button.onClick` | Keeps all view interfaces free of Unity types; enables pure C# mock implementations in edit-mode tests with no Unity runtime dependency | No — this is the foundational convention for all view interfaces in the project |
| 4 | 2026-03-15 | S01/T02 | Two-phase presenter lifecycle (ctor + Initialize) | Constructor only sets fields; `Initialize()` performs event subscription and view setup | Allows async initialization in S02 without changing the constructor signature; separates wiring (Initialize) from injection (constructor) | No — downstream slices build on this lifecycle pattern |
| 5 | 2026-03-15 | S01/T02 | `GameService` placed in `SimpleGame.Core.Services` namespace | Separate `Core/Services/` folder from `Core/MVP/` | Enforces separation of domain logic from MVP infrastructure; domain services can be unit-tested and reused independently of presenter hierarchy | No — establishes the namespace convention for all domain services |
| 6 | 2026-03-15 | S01/T03 | NUnit test framework installation | Added `com.unity.test-framework` to `Packages/manifest.json` (resolved to 1.6.0) | The package was absent from the project; NUnit is delivered via `com.unity.ext.nunit` as a transitive dependency of the test framework — without it `NUnit.Framework` types can't be found at compile time | No — required for all edit-mode and play-mode tests in this project |
| 7 | 2026-03-15 | S01/T03 | Unity `-runTests` batchmode: omit `-quit` flag | Do NOT pass `-quit` alongside `-runTests` | `-quit` races with the async test runner and causes Unity to exit before writing `TestResults.xml`; test runner exits on its own when complete | No — standard usage pattern for Unity batchmode test runs |
| 8 | 2026-03-15 | S01/T03 | MockSampleView disposal test uses call-count tracking | Added `UpdateLabelCallCount` int property to `MockSampleView` | After `Dispose()`, the welcome message label text stays the same if a click fires (same string from `GetWelcomeMessage()`); testing by string equality gives a false pass — call count definitively detects whether `UpdateLabel` was invoked post-Dispose | No — any future mock for `ISampleView` should include a call counter for reliable disposal testing |
| 9 | 2026-03-15 | S02 | ScreenManager does not own presenter lifecycle | ScreenManager only handles scene load/unload; presenter creation/disposal happens in S05 boot flow | Keeps S02 focused on navigation logic (independently testable). Presenter wiring requires view instances found after scene load — mixing concerns here creates lifecycle ordering bugs. S05 connects UIFactory to loaded screen views. | No — ScreenManager is a navigation primitive, not a presenter container |
| 10 | 2026-03-15 | S02 | ScreenId enum over raw strings for screen identification | `ScreenId` enum with `ToSceneName()` helper instead of passing string scene names | Compile-time safety prevents typos, makes the set of screens discoverable, trivial mapping cost. Flagged as an open question in context — resolved in favor of enum. | Yes — add new enum values as screens are added |
| 11 | 2026-03-15 | S02/T01 | ToSceneName helper omitted; enum.ToString() used directly in ScreenManager | `screenId.ToString()` in ScreenManager instead of a static helper on ScreenId | The static-state grep guard (`grep -v "static void|static class|static readonly|static async|static UniTask"`) does not whitelist `static string`, so any static method returning string triggers a false positive. `enum.ToString()` produces identical output for enums whose member names equal scene names. If scene names ever diverge from enum names, reintroduce the helper as a non-static lookup table inside a non-static class. | Yes — revisit if scene names diverge from enum names |
| 12 | 2026-03-15 | S02/T02 | Unity scene creation via batchmode -executeMethod | Static method in `Assets/Editor/` called via `-executeMethod ClassName.MethodName` using `EditorSceneManager.NewScene` + `SaveScene` + `EditorBuildSettings.scenes` assignment | Batchmode -executeMethod is the canonical Unity CLI pattern for editor asset operations; placing the script in `Assets/Editor/` (no asmdef needed) gets it auto-compiled as editor-only via Assembly-CSharp-Editor | No — established pattern for any future editor asset setup scripts |
