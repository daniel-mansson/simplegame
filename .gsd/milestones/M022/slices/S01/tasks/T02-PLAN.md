# T02: SceneSetup + SpawnLevel Wiring

**Slice:** S01
**Milestone:** M022

## Goal

Update `SceneSetup.CreateInGameScene()` to add `HorizontalLayoutGroup` to `DeckPanel` with a `_pieceButtonContainer` child; wire the new field on `InGameView`; remove legacy `DeckLabel`+`PlaceButton` from DeckPanel. Update `PuzzleStageController.SpawnLevel()` to call `SetupDeckPanel`. Re-run SceneSetup and commit.

## Must-Haves

### Truths
- `SceneSetup.CreateInGameScene()` adds `HorizontalLayoutGroup` to `DeckPanel` — `childForceExpandWidth: false`, `childForceExpandHeight: true`, `childAlignment: MiddleCenter`, spacing 8
- `DeckPanel` has a `_pieceButtonContainer` child `RectTransform` with `HorizontalLayoutGroup` and `ContentSizeFitter` (vertical fit: preferred size) that fills the DeckPanel rect
- `InGameView._pieceButtonContainer` is wired to this child via `WireSerializedField`
- `DeckLabel` and `PlaceButton` are NOT created in the InGame scene (legacy placeholders removed)
- `InGameView._deckLabel` and `InGameView._placeButton` `WireSerializedField` calls are removed from SceneSetup
- `PuzzleStageController.SpawnLevel()` calls `_inGameView?.SetupDeckPanel(slotCount)` at the end of spawn (after `SpawnSlotButtons` call — SpawnSlotButtons is removed in S02; for S01 it can remain or be replaced; replacing it now is cleaner)
- SceneSetup is re-run via Unity MCP menu item `Tools/Setup/Create And Register Scenes`
- `Assets/Scenes/InGame.unity` reflects the new layout (DeckPanel active, _pieceButtonContainer child visible)
- All 347 existing tests pass

### Artifacts
- `Assets/Editor/SceneSetup.cs` — updated `CreateInGameScene()`: HorizontalLayoutGroup on DeckPanel, _pieceButtonContainer child, no DeckLabel/PlaceButton
- `Assets/Scripts/Game/InGame/PuzzleStageController.cs` — `SpawnLevel()` calls `_inGameView?.SetupDeckPanel(slotCount)` (replaces or is added after SpawnSlotButtons)
- `Assets/Scenes/InGame.unity` — regenerated by SceneSetup tool

### Key Links
- `SceneSetup._pieceButtonContainer` RectTransform → `InGameView._pieceButtonContainer` field via `WireSerializedField`
- `PuzzleStageController.SpawnLevel()` → `_inGameView.SetupDeckPanel(slotCount)` call

## Steps

1. Read current `SceneSetup.cs` `CreateInGameScene()` to find DeckPanel construction block
2. Remove the `deckLabelGO` (DeckLabel) and `placeButtonGO` (PlaceButton) creation and wiring from `CreateInGameScene()`
3. Add `HorizontalLayoutGroup` to `deckPanelGO`: `childForceExpandWidth: false`, `childForceExpandHeight: true`, `childAlignment: MiddleCenter`, `spacing: 8`, `padding: 4 all sides`
4. Create `pieceButtonContainerGO` as a child of `deckPanelGO`: full-rect stretch RectTransform, add `HorizontalLayoutGroup` (childForceExpandWidth: false, childForceExpandHeight: true, childAlignment: MiddleCenter, spacing: 8), add `ContentSizeFitter` (horizontal: unconstrained, vertical: preferred size)
5. Remove `WireSerializedField(inGameView, "_deckLabel", ...)` and `WireSerializedField(inGameView, "_placeButton", ...)` calls
6. Add `WireSerializedField(inGameView, "_pieceButtonContainer", pieceButtonContainerGO.GetComponent<RectTransform>())`
7. Read `PuzzleStageController.SpawnLevel()` end section — add call `_inGameView?.SetupDeckPanel(slotCount)` after the existing `SpawnSlotButtons(slotCount)` call (S02 removes SpawnSlotButtons; for now call both)
8. Run `lsp diagnostics` to confirm clean compile
9. Re-run SceneSetup via MCP: `mcporter call unityMCP.execute_menu_item --output raw "menu_path:Tools/Setup/Create And Register Scenes"`
10. Check Unity console for errors: `mcporter call unityMCP.read_console --output raw --args '{"count": 30}'`
11. Commit: `feat(M022/S01): deck panel UGUI wiring — HorizontalLayoutGroup with slot buttons`

## Context

- `WireSerializedField` is a local static helper in SceneSetup — already handles null-field warnings
- `_deckPanel` field name on `InGameView` stays the same — it's the outer panel GameObject
- `_pieceButtonContainer` is the new inner RectTransform field that holds the buttons
- SceneSetup re-run overwrites `InGame.unity` — this is expected and correct (K007 rule)
- After SceneSetup runs, verify via MCP console that no `[SceneSetup]` ERROR lines appear
